[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "legacy_extensions: checks frozen pre-split component and system migration evidence",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
