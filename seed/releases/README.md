# ASET Seed releases

Frozen release directories are immutable. Each release directory contains delivery assets, materialized publications, and—where available—a byte-exact expanded tree for direct review.

The current stable documentation release is `0.1-rc11`.

A new release must have a distinct version, exact manifest, checksums, conformance evidence, independent audit, owner approval, and protected tag. Existing frozen bytes must never be rewritten.
