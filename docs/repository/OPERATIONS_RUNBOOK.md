# Specification repository operations

Run `python tools/repository_release_gate.py`. Regenerate derived views with `python tools/generate_repository_views.py`, rebuild the canon package with `python tools/build_canon_package.py`, then rebuild `MANIFEST.json`. No runtime deployment operation is defined by this repository.
