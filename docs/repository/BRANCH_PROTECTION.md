# Recommended branch protection

Protect `main` with:

- pull requests required;
- required semantic CI;
- conversation resolution required;
- force-push disabled;
- branch deletion disabled;
- signed release tags;
- owner approval for normative changes.

Replace `REPLACE_OWNER` in `CITATION.cff` before publication.
